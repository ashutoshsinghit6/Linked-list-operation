# Linked-list-operation
    def add_first(self,e):
        new_node=self._Node(e,None)
        if self.is_empty():
            self._head=new_node
            self._tail=new_node

        else:
            new_node._next=self._head
        self._head=new_node
        self._size +=1
